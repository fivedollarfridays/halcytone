name: ci

# Stub workflow — T3.6 replaces with real lint / test matrix / schema-drift jobs.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: placeholder
        run: echo "stub — replaced by T3.6"
