---
id: T3.6
title: Unified CI workflow
plan: plan-2026-04-sprint-3-monorepo-consolidation
type: chore
priority: P0
complexity: 2
status: done
sprint: '3'
tags:
- ci
- infra
depends_on:
- T3.3
- T3.5
completed_at: '2026-04-18T11:44:26.812093'
---

# Objective

Replace the stub `.github/workflows/ci.yml` scaffolded in T3.1 with a production workflow: `lint` (ruff), `test` (matrix Python 3.11/3.12/3.13 with `pip install -e '.[dev]'` + `pytest -q`), and `schema-drift` (regen schema + fail on diff). No cross-repo auth needed.

# Implementation Plan

Working directory: `/home/kmasty/projects/halcytone/`.

1. Rewrite `.github/workflows/ci.yml` wholesale. Reference template: `halcytone-contracts/.github/workflows/ci.yml`.
2. Jobs:
   - **`lint`** — `actions/checkout@v4`, `actions/setup-python@v5` with 3.11 + pip cache keyed on `pyproject.toml`, `pip install ruff`, `ruff check .`.
   - **`test`** — `strategy.matrix.python-version: ["3.11", "3.12", "3.13"]`, `actions/checkout@v4`, `actions/setup-python@v5` matrix, `pip install -e '.[dev]'` (single-quoted), `pytest -q`.
   - **`schema-drift`** — `actions/checkout@v4`, setup-python 3.11, `pip install -e .`, `python scripts/regen_manifest_schema.py`, `git diff --exit-code halcytone/contracts/bundles/manifest.schema.json`.
3. Triggers: `on: push: branches: [main]`, `on: pull_request: branches: [main]`.
4. `concurrency: group: ci-${{ github.ref }}` with `cancel-in-progress: true`.
5. `fail-fast: false` on the test matrix.

# Acceptance Criteria

- [x] `.github/workflows/ci.yml` has three jobs: `lint`, `test` (matrix), `schema-drift`
- [x] Triggers: push + pull_request to `main`
- [x] `concurrency` group `ci-${{ github.ref }}` with `cancel-in-progress: true`
- [x] `test` job: `pip install -e '.[dev]'`, `pytest -q`
- [x] `test` job matrix includes 3.11, 3.12, 3.13
- [x] `schema-drift` job: `python scripts/regen_manifest_schema.py && git diff --exit-code halcytone/contracts/bundles/manifest.schema.json`
- [x] No `actions/checkout` uses `token:`, `ssh-key:`, or cross-repo `repository:` params
- [x] `fail-fast: false` on the test matrix
- [x] Workflow file parses cleanly (GitHub Actions accepts on push)

# Verification

```bash
cd /home/kmasty/projects/halcytone
python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read())"
# After T3.9 push: gh run list --limit 1 --json conclusion,jobs
```

# Dependencies

- T3.3 (tests must exist for `test` job)
- T3.5 (wiring tests for full coverage signal)