---
id: T3.9
title: Commit + push + open PR
plan: plan-2026-04-sprint-3-monorepo-consolidation
type: chore
priority: P0
complexity: 2
status: done
sprint: '3'
tags:
- ship
- ci
depends_on:
- T3.6
- T3.7
- T3.8
completed_at: '2026-04-18T12:19:33.147427'
---

# Objective

Stage every file produced by T3.2–T3.8 on branch `engage/backlog-sprint-3` in the new `halcytone` repo. Pre-push gates: `pytest` green, `ruff check .` clean, `bpsai-pair security scan-secrets` clean, diff-level secret grep empty. Push, open PR against `main`, wait for CI green on all 5 jobs.

# Implementation Plan

Working directory: `/home/kmasty/projects/halcytone/`.

1. `git checkout -b engage/backlog-sprint-3`.
2. Stage + commit. Can be one commit or logically grouped:
   - commit 1: T3.2 + T3.3 (contracts migration + tests)
   - commit 2: T3.4 + T3.5 (core migration + tests)
   - commit 3: T3.6 (CI workflow)
   - commit 4: T3.7 + T3.8 (CHANGELOG + ROADMAP + README)
   Each commit message follows project style (co-authored-by line).
3. Pre-push gates:
   - `pytest` — ≥356 passed.
   - `ruff check .` — clean.
   - `bpsai-pair security scan-secrets` — "No secrets detected".
   - `git diff main...HEAD | grep -iE "(api[_-]?key|password|secret=|token=|bearer|-----BEGIN)"` — empty.
4. `git push -u origin engage/backlog-sprint-3`.
5. `gh pr create` against `main`:
   - Title: `v0.3.0 — halcytone monorepo consolidation`
   - Body: structural changes summary, migration guide excerpt from CHANGELOG, 5-job test plan.
6. Watch CI. Halcytone-contracts baseline ~20s; 2-minute timeout. Investigate any job hung past 2× baseline. Fix + push if any job fails.

# Acceptance Criteria

- [x] All T3.2–T3.8 work committed on `engage/backlog-sprint-3`
- [x] Pre-push locally: `pytest` all-green (≥356 tests), `ruff check .` clean, `bpsai-pair security scan-secrets` clean, diff-level secret grep empty
- [x] Branch pushed to `origin/engage/backlog-sprint-3`
- [x] PR opened against `main` with descriptive title, summary, and 5-job test plan
- [x] CI run: `lint` pass, `test (3.11)` pass, `test (3.12)` pass, `test (3.13)` pass, `schema-drift` pass
- [x] No new CI warnings vs. halcytone-contracts baseline
- [x] PR body includes the migration snippet from CHANGELOG's `### Migration` section

# Verification

```bash
cd /home/kmasty/projects/halcytone
pytest -q                         # expect ≥ 356 passed
ruff check .                      # clean
bpsai-pair security scan-secrets  # No secrets detected
git diff main...HEAD | grep -iE "(api[_-]?key|password|secret=|token=|bearer|-----BEGIN)" || echo "clean"
# After push: gh run list --limit 1 --json conclusion,jobs
```

# Dependencies

- T3.6 (CI workflow present — push triggers CI)
- T3.7 (CHANGELOG + ROADMAP complete for PR body to reference)
- T3.8 (README done — part of shipped commits)