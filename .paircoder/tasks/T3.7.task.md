---
id: T3.7
title: Unified CHANGELOG + ROADMAP
plan: plan-2026-04-sprint-3-monorepo-consolidation
type: chore
priority: P0
complexity: 2
status: in_progress
sprint: '3'
tags:
- docs
depends_on:
- T3.5
---

# Objective

Fill in the real content of `CHANGELOG.md` and `ROADMAP.md` (scaffolded as stubs in T3.1). Seed CHANGELOG at `[0.3.0]` with structural-consolidation notes and an explicit migration block. Restructure ROADMAP to mark v0.3.0 shipped and narrow v0.4.0/v0.5.0 to fusion logic + second consumer.

# Implementation Plan

Working directory: `/home/kmasty/projects/halcytone/`.

1. Write `CHANGELOG.md` (Keep-a-Changelog v1.1.0):
   - `## [Unreleased]` (empty).
   - `## [0.3.0] — 2026-04-18`:
     - `### Changed` — structural consolidation; import-path rename (`halcytone_contracts` / `halcytone_core` → `halcytone.contracts` / `halcytone.core`); removed cross-repo `_EXPECTED_CONTRACTS_VERSION` dance in `halcytone.core`; `__contract_version__` bumped to `0.3.0`.
     - `### Added` — nothing new functionally; 24-name public surface unchanged from v0.2.0.
     - `### Migration` — concrete rules: `from halcytone_contracts import X` → `from halcytone import X`; drop any `halcytone-contracts @ git+...` dep lines; adopt `halcytone>=0.3,<0.4`. Under 0.x sharpened policy, `check_contract_version("0.1.x")` and `("0.2.x")` both hard-fail against v0.3.0.
   - Preserve historical `[0.2.0]`, `[0.1.1]`, `[0.1.0]` sections under `## Pre-consolidation (halcytone-contracts)` header with a note pointing to the archived repo.
   - Comparison links at bottom.
2. Write `ROADMAP.md`:
   - v0.1.0 shipped (pre-consolidation, archived).
   - v0.1.1 shipped (Apache-2.0 relicense, archived).
   - v0.2.0 shipped (typed manifest, archived).
   - v0.3.0 shipped (monorepo consolidation, this release).
   - v0.4.0 — real fusion logic in `halcytone.core`: session state machine (Preflight → Baseline → Active → Teardown), LSL ingest, 200 Hz StateVector emission, writing `state.jsonl` + `SessionManifest`, DDL bootstrapper.
   - v0.5.0 — second consumer: `halcytone.sensors` (LSL adapters + simulator) or `halcytone.audio` (mapper + synth). Decide at v0.4.0 ship.
   - v1.0.0 — stability target; pin recommendation switches to `halcytone>=1.0,<2.0`.
3. Both files link to `https://github.com/fivedollarfridays/halcytone-contracts` (archived) and `https://github.com/fivedollarfridays/halcytone-core` (archived).

# Acceptance Criteria

- [ ] `CHANGELOG.md` has a `## [0.3.0] — 2026-04-18` section with `### Changed` and `### Migration` subsections
- [ ] Migration section lists the import rewrite, pin replacement, and new pin recommendation (`halcytone>=0.3,<0.4`) explicitly
- [ ] Historical `[0.2.0]`, `[0.1.1]`, `[0.1.0]` sections preserved under a `## Pre-consolidation (halcytone-contracts)` heading
- [ ] CHANGELOG comparison links resolve (may point at archived repo URLs for pre-0.3 sections)
- [ ] `ROADMAP.md` has sections for v0.1.0, v0.1.1, v0.2.0, v0.3.0 shipped + v0.4.0 (fusion) + v0.5.0 (second consumer) + v1.0.0 stability
- [ ] Both files include a link to the archived `halcytone-contracts` and `halcytone-core` repos
- [ ] No remaining "7-repo fleet" or "halcytone-contracts as separate repo" language in ROADMAP

# Verification

```bash
cd /home/kmasty/projects/halcytone
grep -c "^## " CHANGELOG.md          # expect ≥ 5
grep -c "^## " ROADMAP.md            # expect ≥ 7
grep -c "archived" CHANGELOG.md ROADMAP.md  # expect ≥ 2
```

# Dependencies

T3.5 (need full picture of what landed to write accurate release notes).