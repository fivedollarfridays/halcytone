---
id: T3.5
title: Port + expand core tests
plan: plan-2026-04-sprint-3-monorepo-consolidation
type: refactor
priority: P0
complexity: 4
status: done
sprint: '3'
tags:
- migration
- tests
- forcing-function
depends_on:
- T3.3
- T3.4
completed_at: '2026-04-18T11:06:29.922349'
---

# Objective

Port the existing 14 `halcytone-core/tests/test_wiring.py` tests into the new monorepo's `tests/` dir (rewriting imports), then fold in the 3 T2.6–T2.8 wiring tests that the canceled core-sprint-2 was supposed to deliver. This sprint becomes the forcing function that proves the typed manifest is consumable from a real caller.

# Implementation Plan

Working directory: `/home/kmasty/projects/halcytone/`.

1. Copy `halcytone-core/tests/test_wiring.py` → `halcytone/tests/test_wiring.py`.
2. Rewrite imports: `from halcytone_core.wiring import ...` → `from halcytone.core.wiring import ...`; `import halcytone_core` → `import halcytone.core`; `from halcytone_contracts import X` → `from halcytone import X`.
3. Add three new tests (scope from canceled T2.6–T2.8):
   - `test_session_manifest_full_typed_payload_round_trips`: construct `SessionManifest(session_id=..., started_at=..., baselines=Baseline(streams={3+ real stream names: hrv.rmssd, breath.rate, eda, ...}), summary=SessionSummary(all 9 numeric fields + summary_schema_version=1), ...)`, `model_dump_json()`, `model_validate_json()`, assert equality.
   - `test_baseline_rejects_unknown_stream_through_manifest`: construct a `SessionManifest` with a `Baseline.streams` key `"not.a.real.stream"`; assert `ValidationError`; assert the error message contains `"not.a.real.stream"`.
   - `test_check_contract_version_hard_fails_on_0_1_x_pin`: call `check_contract_version("0.1.0")` from a core-side import; assert `ContractError`; assert message contains "pre-1.0". Also verify `check_contract_version("0.3.0")` is a no-op.
4. Verify: `pytest tests/test_wiring.py -q` passes, ≥20 total. Full suite still green (≥356 tests).

# Acceptance Criteria

- [x] `tests/test_wiring.py` exists under the new repo
- [x] Existing 14 wiring tests ported cleanly with imports rewritten
- [x] New test: full-payload `SessionManifest` with multi-stream `Baseline` (≥3 streams) + complete `SessionSummary` (every numeric field set), JSON round-trip, equality
- [x] New test: `Baseline` with unknown stream key raises `ValidationError`; message contains the offending stream name
- [x] New test: `check_contract_version("0.1.0")` raises `ContractError`; message mentions "pre-1.0"
- [x] New test: `check_contract_version("0.3.0")` is a no-op (no warning, no raise)
- [x] `pytest tests/test_wiring.py -q` passes with ≥20 tests
- [x] Full `pytest -q` green with ≥356 tests (336 contracts + ≥20 wiring)
- [x] `ruff check tests/test_wiring.py` clean

# Verification

```bash
cd /home/kmasty/projects/halcytone
grep -r "halcytone_core\|halcytone_contracts" tests/test_wiring.py || echo "clean"
pytest tests/test_wiring.py -v 2>&1 | tail -5
pytest -q                         # expect: ≥356 passed
ruff check tests/
```

# Dependencies

- T3.3 (contracts tests ported; shared layout)
- T3.4 (core source migrated — wiring tests import from `halcytone.core.wiring`)